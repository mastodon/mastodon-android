<?xml version="1.0" encoding="utf-8"?>
<!-- masto.nyc fork: replaces upstream's Mastodon logo vector. The PNG already carries the
     108dp canvas and adaptive-icon safe-zone padding, so this just fills the layer. -->
<bitmap xmlns:android="http://schemas.android.com/apk/res/android"
	android:src="@drawable/ic_launcher_elephant"
	android:gravity="fill"/>
